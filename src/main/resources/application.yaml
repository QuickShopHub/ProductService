spring:
  datasource:
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
    url: ${DB_URL}
  redis:
    connection:
      standalone:
        host: localhost
        port: 6379
  data:
    redis:
      repositories:
        enabled: false